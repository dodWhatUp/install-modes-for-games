# Cyberpunk 2077 — History

## 2026-09-14 — Unified control package prepared; monthly updates enabled

- User selected one common OptiScaler/ReShade control surface for SR, RR, FG/MFG and NR, live SR/NR adjustments, latest compatible runtimes/models with SR **L** rather than M, and automatic maintenance **once a month**. Kept the existing OptiScaler consumer and CET adapter; no ReShade or second FG implementation was added. This explicit direction supersedes the earlier preference to leave native DLLs untouched and put FG controls outside OptiScaler.
- Built a separate source worktree at [OptiScaler NR v0.8.3](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/releases/tag/v0.8.3), commit `47e134cbcd9d3653d63f58a36d38281e827007ec`, preserving the installed v0.7.7 source and snapshots. Ported personal slots 0–3 and F6/F7/F10/F11/F12/Delete; F8/Page Up/Page Down remain unbound. Upstream removed some experimental NR fields; saved values are retained for rollback but not applied to nonexistent controls. New private-upscaler fields migrate with defaults.
- [Prepared source and adapter](../../examples/cyberpunk-2077/UnifiedControls/README.md): F12 common SR quality/custom-scale, RR, native FG/MFG/Dynamic target and upstream NR controls. Native-setting requests use a short-lived session token, replay protection, cooldown, delayed readback and rollback on rejection. F6 retains DLAA → Quality → Balanced → Performance. The custom SR slider requests equal native DRS bounds between 50% and 100%; **actual Cyberpunk render-size changes are not yet verified**. The panel reports input/output dimensions separately from requested settings. Some engine backend changes still require restart; DLL updates always require the game closed.
- Downloaded the official production [Streamline 2.14.1 SDK](https://github.com/NVIDIA-RTX/Streamline/releases/tag/v2.14.1), published 2026-09-08. ZIP SHA-256 `92C4D954631A1710DA86CA3FA8D5034F2B9503838C95FC4AE977AE149319781B` matched; all eleven candidate NVIDIA DLL signatures are Valid. Its SR/RR/FG runtimes are 310.9.1; preserve the installed signed NR 310.8 runtime as current upstream directs. NR hint numbers are not release/model rankings.
- Candidate config pins SR L and RR F, keeps native Streamline/DLSSG as the only FG implementation (`Enabled=false`, `FGInput=nofg`, `FGOutput=nofg`), and exposes native multiplier/Dynamic target through OptiScaler hooks. v0.8.3 removed the old `External` switch. The per-game NVAPI utility snapshots eight settings, removes competing driver MFG/SR/RR overrides, and retains FG model B. No global profile changes are planned.
- Correction to older guidance: Streamline 2.14.1 explicitly adds Dynamic MFG support with V-Sync and frame rate limiters. The older launch restriction is historical, not universal. Runtime integration and NR stability still require local testing; a release note is not a Cyberpunk test result.
- Checks passed: x64 C++ build; independent personal-slot roundtrip, migration/retired-value preservation and corrupt-file handling; Lua adapter prerequisites/quality/scale/RR/FG/replay/expiry/rollback/onDraw tests; reverse source-patch application; NVIDIA signatures; proxy export contract; static Streamline import compatibility. Current proxy definition's 854 declared names and explicit ordinals are present (938 total exports). Fourteen incidental FSR exports disappeared versus the old custom build. New `sl.common.dll` removes 58 NGX exports, none statically imported by examined retained root binaries/replacement plugins; dynamic lookups still need runtime validation.
- **Installation not performed:** Batman and Steam were running. The guarded installer stopped before any game or driver mutation, and a request to close them at a safe point is pending. Current Cyberpunk remains the preceding personal-slots build (`A6B69CCAD9F2176B6255365BDD651395459BEC7BF9F26A066DD82BB0B2A2A270`), native SR/RR/FG 310.1 and Streamline 2.7.1. Do not mark the prepared package installed or working.
- Final candidate proxy SHA-256 `121D0D5D86A94D04A3DDC1C5C819BBF25554E8824982ECB0E130F1C74CEAA27F`, size 26,374,144 bytes. Rebuilt after the panel was bounded to the display and its scale/model labels were made responsive to current settings. Re-ran proxy/static-import and portable adapter checks; regenerated candidate; all fourteen installed originals still match the pre-installation validation copies.
- Private staged package: `cyberpunk-unified-20260914`, with a fourteen-file candidate manifest, official download, build/tests and guarded installer. A read-only rollback validation copied/verified the fourteen current original files. Actual installation must first make a new just-in-time snapshot of files, NR slots, UserSettings, Steam state and NVIDIA profile/overlay state. The generated `Restore-UnifiedControls.ps1 -Apply` restores those fourteen files and the eight exact driver settings while preserving NR slot JSON and saves. No new install snapshot exists yet; no rollback ran.
- Monthly task `monthly-cyberpunk-graphics-updates` is active on the **14th at 12:00 local time**. It only installs compatible verified updates while the game/store/compilers are closed, preserves custom controls/models/slots and rollback, stays quiet without an actionable change, and never launches a game automatically. This schedule is explicitly authorized by the user's monthly reply; it does not authorize resuming other paused game experiments.
- Next boundary: once Batman/Steam are closed, refresh process/config inventory, regenerate the candidate against current INI, snapshot and install, verify fourteen hashes and unchanged protected files plus driver readback, then test one graphics group at a time in a repeatable Cyberpunk scene with fresh pre-test saves. Check SR dimensions, RR prerequisites, reported MFG/Dynamic support, NR save/recall, transitions, alt-tab and shutdown. Do not infer working FG from an overlay or total FPS alone.

## 2026-09-13 — Follow-up: possible NVIDIA App conflict with F6

- User asked whether NVIDIA App could conflict with the native DLSS quality shortcut. Read-only inspection of the current Cyberpunk profile again showed Super Resolution Mode = **Global - Use 3D app setting**, model overrides FG B / SR L / RR F, Dynamic MFG up to 6x, and Smooth Motion off.
- NVIDIA's [override documentation](https://www.nvidia.com/en-us/geforce/news/nvidia-app-global-dlss-overrides-rtx-40-series-smooth-motion/) distinguishes model selection from an override of SR input resolution. A forced Super Resolution Mode could supersede the game's intended quality/scale; no such forced mode is visible in the inspected profile. A model override alone is not evidence that quality cycling is blocked.
- Inference: Dynamic MFG targeting refresh rate can hide a base-FPS change in the displayed-FPS counter. F6 must be checked against native quality readback and real render dimensions, not total FPS alone. The CET notification-event error remains the directly evidenced defect; no NVIDIA/F6 conflict has been established. No game/driver settings changed in this follow-up.

## 2026-09-13 evening — Personal NR slots, simplified shortcuts and NVIDIA overlay recovery

- User reported that saving settings under F11 model presets did not preserve separate configurations, F6 produced no visible result, and NVIDIA statistics disappeared. They explicitly selected **personal settings per number**. They removed F8 and Page Up/Page Down and reaffirmed F7 25%/100% only.
- Evidence from the latest pre-repair run: F11 changed the numeric model hint and NR evaluation logged the changed hint, but the old implementation had one shared configuration rather than four user profiles. F7 logged only 0.25/1.0 and evaluated corresponding model dimensions (post-SR 960×540/3840×2160). The F6 adapter log contained `Tried to call ImGui from invalid event!`; its notifications were incorrectly called from onUpdate. Native status and UserSettings both ended at Performance, but that alone does not prove F6 recreated render targets.
- NVIDIA App 11.0.9.251 showed **Enable and open overlay** and no overlay process was running. Re-enabled that control; UI then showed **Alt+Z - Open overlay**, overlay processes started, and the statistics strip showed FPS/GPU/CPU/latency at the subsequent Cyberpunk startup. No evidence attributes the disabled setting to OptiScaler. Current OptiScaler overlay-block flag was auto/default false; no blocker change was needed.
- Evening graphics inventory differs from the old install baseline: native DLSS Performance, saved MFG x4, HDR10 PQ and DRS off. NVIDIA App displayed Dynamic MFG targeting max refresh, up to 6x, SR override using the game's setting, Smooth Motion off, and global RTX HDR on. Native/driver settings were preserved. Do not derive base FPS by dividing displayed FPS by a fixed multiplier in this profile.
- Implemented [personal slots](../../examples/optiscaler-dlssnr/personal-slots/README.md): F11 recalls 0–3; full/compact Save Settings saves the selected NR slot in `DlssNrProfiles.json` and normal OptiScaler INI. Model hints are a separate field. All slots begin from the current configuration. Unsaved changes are visible. Full-slot validation precedes application; profile-file replacement is atomic and corrupt files are preserved. One-second switching guard retained.
- Removed bindings with `PlacementToggleKey=-1`, `FpsShortcutKey=-1`, `FpsCycleShortcutKey=-1`, `ShowFps=false`; retained `ScaleCycleKey=0x76`. Manual placement controls remain. F6 notifications queue for CET onDraw and retain the most recent result in `sr-last-result.txt`.
- Snapshot: private `cyberpunk-personal-controls-20260913-232201`, with prior proxy/INI/adapter, root executable/native hashes and versions, logs, UserSettings, Steam state, NVIDIA DRS/overlay registry, source-before files, and pre-test saves. The prior baseline snapshots remain intact. Cyberpunk/launcher/Steam/build processes were closed before mutation.
- Changed three game files: `dxgi.dll`, `OptiScaler.ini`, `plugins/cyber_engine_tweaks/mods/GraphicsHotkeys/init.lua`. New proxy SHA-256 `A6B69CCAD9F2176B6255365BDD651395459BEC7BF9F26A066DD82BB0B2A2A270`; stamp `e237f89+personal-slots`. All 946 exports match the previous proxy; 57 other executable/DLL files remained unchanged.
- Tests passed: x64 build/link; standalone C++ independent-slot roundtrip including inherited pass settings, restart without startup mutation, cooldown, invalid-slot atomic rejection and corrupt-file preservation; Lua native-setting cycle/guards/rollback/channel tests plus enforcement that notifications only draw in onDraw. New build started and logged NR creation/evaluation/timing; no new invalid-ImGui-event line was observed at startup.
- Runtime boundary: game was launched for testing, but the user was using the desktop. Requested availability before taking control for slot save/recall and F6 render-size checks. Those interactive checks remain pending; no claim of complete gameplay stability or F6 renderer recreation follows from unit tests/startup.
- Rollback: close Cyberpunk/Steam/launchers; run this snapshot's `Restore-PersonalControls.ps1 -Apply`. Dry-run passed. It preserves current files, restores the preceding three files and preserves personal JSON; NVIDIA overlay stays enabled. Run this before the older F7 rollback chain. No rollback performed and no reminder scheduled.

Sources for FPS interpretation: [NVIDIA FrameView 2.0 guide](https://images.nvidia.com/content/geforce/technologies/frameview/frameview-2-0-user-guide-web-version.pdf), [NVIDIA App overlay controls](https://www.nvidia.com/en-us/geforce/news/nvidia-app-download-and-features/), and [NVIDIA's output-FPS explanation](https://forums.developer.nvidia.com/t/frame-generation-does-not-work/271646/4).

## 2026-09-13 — F7 two-value controls installed

- User requested replacing every installed NR resolution cycle with F7 toggling only 25% and 100%, and live DLSS SR selection beside NR where possible.
- Same game builds, rendering APIs, RTX 5070 Ti/616.56 and existing hook owners as the preceding inventory; live desktop 3840×2160 at 150 Hz. No game or helper was running; Steam was shut down gracefully before mutation.
- Current-state snapshot: private `f7-controls-20260913/before-f7-20260913-141908`, with before/after file hashes, root hook/executable version/hash inventory, INIs, CET configuration, UserSettings, per-app Steam state, launch configuration, NVIDIA DRS and graphics environment. Prior stock/known-good snapshots remain separate.
- Full rebuilds used MSVC 14.44.35207 / Windows SDK 10.0.26100.0. Both preserve all 946 named exports/ordinals. [Follow-up source patches](../../examples/optiscaler-dlssnr/f7-two-scale/README.md) preserve the exact code changes without binaries.
- New proxy `298D93C3D648330E279B614FC4B822FE38D87EC33516EFED90537664E9E9A090`, stamp `e237f89+f7-25-100`; `[DlssNr] ScaleCycleKey=0x76`. Native SR adapter moved from F7 to F6 (`117 << 48`); F9 is already Quick Load and is preserved. F6 is the agent's collision-avoiding choice, not an explicit user key selection. F8 retains placement.
- F12 compact panel adds a native DLSS section and cycle button. A narrow, timestamped file request reaches the existing CET update-event adapter; it validates DLSS/player state, fixed-resolution mode, request age, cooldown and native setting readback. It does not invent an SR ratio or edit UserSettings directly. Status is read from the native setting.
- Lua tests passed for cycle order, rapid presses, unloaded player, wrong upscaler, active DRS refusal, overlay guard, rejection/apply rollback, restart cleanup, panel requests, one-time consumption, stale/future/malformed rejection and native status. Installed CET source confirms the sandbox supports the used io/os APIs. Four game files changed: proxy, OptiScaler.ini, adapter Lua and bindings.json.
- Verification: target hashes matched after installation, protected native files unchanged; restore dry-run passed before and after mutation. No new game launch or live keyboard test. Earlier runtime evidence is historical, not proof for this revision.
- Rollback: close games/Steam/helpers; run the snapshot's `Restore-F7.ps1 -Game Cyberpunk`. It validates targets/backups, preserves the current files and restores only this revision's changes. Run it before older rollback scripts because those expect the previous hashes. No rollback executed.
- Next boundary: verify F7 key release, notification, actual 25%/100% model sizes, recreation and Save Settings reload in a repeatable scene. Test F6 and the compact-panel button separately from F7/F8, checking native-menu values and actual input dimensions. DRS is available in native settings but was not enabled. No reminder or automatic resume scheduled.

## State recorded before the F7 revision

- State: OptiScaler-DLSSNR 0.7.7 extended-controls build installed as `dxgi.dll`; CET 1.37.1 plus GraphicsHotkeys supplies F7 native DLSS quality. Conservative one-pass pre-SR profile retained. Original-build log shows neural creation/evaluation-path activity; extended hotkeys and CET integration await in-game testing.
- Last known-good: clean Cyberpunk 2077 2.31 / Steam build 20383525 file and settings state captured immediately before installation. A fresh stock gameplay launch was not performed during this task.
- Paused/resume boundary: when the other active game is closed, test Delete/F12 menu transitions, F10/F11, then F9 NR scale, F8 SR placement, and F7 native quality separately in a repeatable scene. Check native-menu readback and effective render/model dimensions. Isolate native FG if changes destabilize the game. No automatic launch or reminder was scheduled.
- Reminder policy: no reminder scheduled; resume only on explicit request.

## 2026-09-13 — F9 NR resolution, F8 placement, F7 native DLSS quality

- User explicitly authorized implementation after asking for live resolution/placement/quality hotkeys. Existing Delete/F10–F12 behavior is preserved; F11 continues to cycle numeric NR model hints, not combined quality presets.
- F9 implementation: configurable `ScaleCycleKey=0x78`; cycles working scale 1.0, 0.75, 0.5, 0.25 and wraps. Scale applies per image dimension, relative to the NR input selected by placement, not the output display. F8: `PlacementToggleKey=0x77`; flips `RunBeforeSR`, refuses while Finished Picture or Deferred DLSS is active, and explains combined RR+SR placement in its notification. Both share a one-second debounce and reuse existing model-recreation/feature-parking logic. Compact UI exposes before-SR placement.
- OptiScaler build: full MSVC 14.44.35207 / SDK 10.0.26100.0 rebuild after Config layout changes; compile/link succeeded. Stamp `e237f89+f7f9`, proxy SHA-256 `4400E00C58451AAC69554600AEFB178089C4D9D67C6BCF9C8BFEA8FE961557BE`. All 946 names/ordinals match the preceding proxy. Complete extended patch saved separately from the earlier compact-only patch and reverse-apply checked against staging.
- F7 required a game-specific native-settings adapter. Installed official [CET 1.37.1](https://github.com/maximegmd/CyberEngineTweaks/releases/tag/v1.37.1) plus the authored GraphicsHotkeys Lua. The release supports the 2.31 game line; its package SHA-256 matched GitHub's digest: `1855017796A27F518199F5B7D7210EF1DB7A5C5F0AF468C68E04E6E666AD248C`. CET uses `version.dll`, with no pre-existing collision; OptiScaler retains `dxgi.dll`. Different filenames are not proof of runtime compatibility. No RED4ext/redscript or additional gameplay mods were added.
- F7 implementation: DLAA → Quality → Balanced → Performance; Auto/unknown starts at Quality. Queues native `/graphics/presets` DLSS changes to the update event, checks DLSS is selected and a player exists, confirms changes, delays readback by 0.5 seconds, and reports status. Two-second cooldown; CET overlay blocks F7. Rejection/apply failures attempt to restore the previous value. No graphics changes on startup, direct JSON edits, or `RequestSaveUserSettings` calls. Native readback alone cannot prove renderer recreation.
- CET bindings: F7 quality; Insert CET overlay. Optional CET graphics/gameplay patches explicitly false. All package support files retained; CET's LICENSE installed under its plugin directory to avoid overwriting the existing OptiScaler LICENSE. Exactly 22 new files are in the ownership manifest, including local configuration/bindings/Lua.
- Just-in-time snapshot: private `cyberpunk-before-f7-f9-20260913-131843`, eight saved files including old DLL/INI/log/license, UserSettings, Steam manifest/config/localconfig; 14 protected executable/native/runtime hashes plus the planned CET ownership manifest. Prior clean and compact-only snapshots preserved. Cyberpunk and its launchers were not running. A Plague Tale: Requiem and its unrelated build work were left alone; Steam configs were captured read-only, not mutated.
- Mutation checkpoints: replaced the unloaded OptiScaler proxy and added exactly two INI lines; 14 protected files unchanged. Then installed and verified the CET payload, loader last. UserSettings and existing OptiScaler LICENSE unchanged. Final INI SHA-256 `FEBF38989D8B9EF630596316E8C01443FCCB3A941ECFEC905AB3A04A5F7A369F`; NVIDIA 310.8 NR runtime signature still Valid.
- Positive tests: complete C++ build/export checks; source-patch validation; Lua syntax and mock tests for cycle order, rapid presses, overlay/player/upscaler guards, rejection rollback, apply-failure rollback, recovery, and startup non-mutation. Installed bindings decode to F7 (118) and Insert (45). Guarded rollback passed WhatIf validation before and after installation.
- Runtime limitation: no Cyberpunk launch or interactive hotkey test, because another game was active. No claim of observed CET load, correct render-buffer resizing, visual improvement, stable NR placement switching, or safe rapid FG interaction. Current result is installed/static-tested, not gameplay-validated.
- Exact rollback: close Cyberpunk/RED launcher; run snapshot `restore-f7-f9.ps1`. It validates saved hashes and current ownership, preserves current controls, quarantines all recorded CET files (allowing preserved changes to CET config/bindings), and restores the preceding `D563566B…` proxy / `E653CF1F…` INI. Changed immutable payload files cause a stop for inspection. It leaves native files, Steam/UserSettings, unrelated mods, and generated logs untouched. For complete removal, follow with the earlier compact rollback, then the original installation rollback in that order.
- Defaults saved for future compatible profiles: F8/F9 extended controls and F7 through a validated native adapter. Other games were not blindly migrated, and Cyberpunk-specific CET/Lua was not installed elsewhere. Next boundary is the separated live test described above; no reminder scheduled.

## 2026-09-13 — Delete / F12 / F11 / F10 compact controls

- User-selected action: Delete for the full menu; F12 for a small DLSS Neural Rendering-only window; F11 to cycle predefined model hints; F10 to toggle NR. Save the mapping as the cross-game default for compatible installations.
- Implementation: locally patched upstream v0.7.7 commit `e237f895623742b761f9e5f00067cb3dc62619f4`; configurable `[DlssNr] PresetCycleKey` and `CompactMenuKey`, shared full/compact input capture, numeric preset-cycle notification, and compact on/off/preset/style/intensity/scale/pass controls with Save Settings.
- Model preset decision: cycle the fork's exposed hints 0–3. Their visual meanings are undocumented; no Quality/Performance labels or claimed model effects were invented.
- Final UI refinement: Working scale, like Intensity, commits on slider release. The initial custom pair was preserved under the snapshot's `before-slider-refinement` folder before replacing the proxy with this refinement; the saved INI was unchanged.
- Build result: MSVC 14.44.35207 / Windows SDK 10.0.26100.0, x64 Release. The earlier interrupted build left empty and zero-header COFF objects; those were rebuilt. Final compile and link succeeded. No replacement occurred until final DLL validation passed.
- Current-state snapshot: private `cyberpunk-before-compact-controls-20260913-123542`, including the previous proxy/INI/log, current user settings, Steam manifest/configuration, and SHA-256 manifests. The clean pre-install snapshot remains preserved separately.
- Mutation: replaced only `dxgi.dll` and changed exactly four INI entries: `Menu.ShortcutKey=0x2e`, `DlssNr.ToggleKey=0x79`, `DlssNr.PresetCycleKey=0x7a`, `DlssNr.CompactMenuKey=0x7b`. Steam was shut down gracefully before mutation.
- Positive static evidence: final proxy SHA-256 `D563566B3B8BCF7CC895C805A64CDEA4F351CE31480ACB2B5A9A58FD0300AB0B`; 946 export names and ordinals identical to the release; all 14 protected game/runtime files unchanged; UserSettings unchanged; NVIDIA 310.8 runtime signature still Valid; guarded restoration passed a non-mutating WhatIf run.
- Additional historical runtime evidence discovered: the original proxy's 11:53–11:54 log recorded a model feature created, ready after submission, pre-RR+SR composition with one pass at 1920×1080, and elapsed timing of 4.83 ms (4.74 ms model). The source warns timing intervals can include other GPU work. Visual output, scene quality, long-run stability, and clean shutdown were not observed in this task.
- Scope of default: saved in `preferences/GENERAL.md` and a reusable INI/source patch. Hogwarts retains its different game-specific OptiScaler build; it was not silently replaced with this fork. Future migrations must port/validate equivalent controls against that game's actual build.
- Runtime result for custom controls: not tested in-game. Build/static success does not establish keyboard handling, mouse capture, visual correctness, or stability in Cyberpunk.
- Exact controls rollback: exit game, launcher, and Steam; run the snapshot's `restore-compact-controls.ps1`. It checks the old-file hashes and installed proxy identity, preserves the current DLL/INI, then restores the original pair. To remove the entire mod afterwards, use the earlier installation snapshot's guarded restore script. The earlier exact-hash manifest cannot directly accept the custom proxy.
- Next boundary: verify menu transitions and controls in Cyberpunk, then NR output/performance in a repeatable scene. No reminder scheduled.

## 2026-09-13 — OptiScaler with DLSS Neural Rendering for RTX 50

- Goal: install OptiScaler with a DLSS 5 / DLSS Neural Rendering extension for an RTX 5070 Ti while preserving Cyberpunk's native DLSS SR, RR, and MFG paths.
- Baseline: Cyberpunk 2.31, Steam build 20383525, 64-bit D3D12, RTX 5070 Ti 16 GB, driver 616.56, Windows build 26200, 3840×2160 borderless, HDR off. No local proxy, ReShade, CET, RED4ext, archive mod, or script mod was present.
- Native profile: DLSS Transformer/Auto, DLSS Frame Generation x2, Reflex enabled; global ray tracing off. Native DLSS 310.1 and Streamline 2.7.1 files were present.
- Selected path: current OptiScaler-DLSSNR-PreSR-Multipass v0.7.7 release, complete package, `dxgi.dll` proxy, original NVIDIA-signed 310.8 RTX 50 neural runtime, one FP8 pass before SR at scale 1.0.
- Ownership: Cyberpunk retains SR/RR/FG/MFG. OptiScaler's internal FG, replacement output, deferred SR, residual-across-RR, residual FG, finished-picture path, multipass, and auto-capture are off.
- Snapshot: private `cyberpunk-before-optiscaler-dlssnr-v077-20260913-042323` snapshot contains native executable/DLSS/Streamline files, user settings, Steam state, pre-install hashes, installed ownership manifest, and guarded rollback script.
- Positive evidence: release ZIP matched its published SHA-256; all 63 entries covered by the release's internal checksum list matched; `dxgi.dll` identifies as OptiScaler 0.7.7; the forwarder matched its release hash; the RTX 50 runtime matched `E16BCF…FC8E` and had a valid NVIDIA signature; all captured native game files and `UserSettings.json` remained byte-for-byte unchanged; 66 installed files were inventoried.
- Rejected input: a different local file also labeled 310.8 had SHA-256 `4B8D19…BA05` and an NVIDIA `HashMismatch` signature. It was not installed. Version strings alone are not provenance.
- Runtime result: untested. No claim is made that the proxy loaded, the neural feature was created, or frames were evaluated.
- Known current risk: community reports describe Cyberpunk device removal when NR or model scale is toggled while native FG is active; avoid rapid toggling and isolate FG if a crash occurs.
- Rollback performed: no; the requested profile remains installed.
- Root cause: not applicable. Runtime behavior is unknown until gameplay testing.
- General lesson promoted to: existing repository policy already requires exact hashes and signature checks; this task supplies a concrete game-specific example.
- Safe next options: (1) runtime test with current DLSS + native x2 FG, (2) repeat with native FG off if unstable, (3) only then test global RT/RR as a separate change, or (4) guarded rollback to the clean snapshot.

## 2026-09-14 — Library comparison review

Added [categorized comparisons](COMPARISONS.md) covering manager/foundations, performance, graphics/DLSS, QoL/UI, animation, AI, abilities/mechanics and actual content extensions. No game files changed or new gameplay tests were performed by this review. Earlier local evidence remains stronger than generic recommendations; historical control mappings and unresolved validation boundaries are preserved. See the [14-game guide](../../docs/INSTALLED-GAMES-MOD-GUIDE.md) for scope and installed-library checks.
