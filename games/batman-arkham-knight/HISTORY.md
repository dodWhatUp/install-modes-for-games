# Batman: Arkham Knight History

## 2026-09-14 — Live activation diagnosis

- User reports that DLSS 5 and DLSS activation appear not to work. Inspected the already-running game without replacing files or changing graphics settings. Steam build 13250859, RTX 5070 Ti and driver 616.56 remain unchanged; the three active OptiScaler/ReShade/Luma binary hashes match the prior tested profile.
- **Local runtime evidence:** the 00:17 launch created DLSS through the DX11-to-DX12 bridge successfully at 00:18:51, with 3840×2160 render/output and quality 5 (DLAA). NR created at 00:18:53, logged engine guides, after-SR processing, model 960×540, preset 0, one pass and residual composition. Another DLSS creation succeeded at 00:20:13. Repeated NR timing records continued through 00:32:23, with no NR creation/evaluation failure found in the inspected log. This establishes active DLAA/NR processing; it does not establish the user's intended visual result or working hotkeys in this run.
- **Visible UI:** the Home/Luma Settings page showed Super Resolution Auto and DLSS Preset L. The previously enlarged text is visibly rendered. A subsequent menu-input request and refresh were rejected because the computer-use helper already had an active request; F12/F10 could not be rechecked in this turn. No particular warning was visible in the captured Luma page.
- **New configuration drift:** LumaBase.ini now enables DLSS5_Feed and Lumenite_Kernel, in that order, with the default provider 0. The old Feeder add-on remains `.repair-disabled` and was absent from the loaded-module inventory. These leftover effects do not activate the current Luma/OptiScaler neural consumer; their enabled state differs from the prior empty Luma preset. No second Feeder consumer was found. Do not infer that these effects caused an NR activation failure: current NR dispatch is successful.
- **Feature distinction:** DLSS Quality/Balanced/Performance and F6 remain absent from this installed native-resolution Batman integration. F7 changes NR dimensions only. Home exposes ReShade/Luma; F12 is the configured compact NR menu and F10 is the configured NR toggle. Current upstream Luma wiki still marks Batman work in progress.
- Preserved running-session log/configuration copies and binary hashes in private `activation-audit-20260914-003232`. These are diagnostic copies, not a closed-game rollback snapshot. No graphics mutation or rollback occurred. Next boundary: identify the specific failing control or desired mode; if removing the obsolete effects is selected, snapshot the live settings immediately before a reversible edit. Do not reinstall the disabled Feeder chain or treat missing lower-resolution SR as an NR activation failure.

## 2026-09-13 — Menu readability and DX12 warning follow-up

- User reports a highlighted DX12/no-running-DX12-game warning when pressing Home and unreadably small overlay text. Steam now lists Batman in another library; rediscovered the live installation through libraryfolders.vdf. Build 13250859 and executable/OptiScaler/ReShade/Luma hashes still match the tested profile. Historical hard-coded installation paths must not be reused blindly.
- Latest pre-change local run (22:09:55–22:12:55) confirms DLSS w/Dx12 creation success, 3840×2160 input/output with quality 5 (DLAA), NR model 960×540 after SR, repeated NR timings, and an F11 change from preset 0 to 1 with model recreation. Both components logged shutdown. This disproves a blanket claim that this DX11 game cannot run the installed DLAA/NR path; it does not verify every overlay option.
- Located the exact string `This option needs a native DirectX 12 game.` in the installed OptiScaler binary and matching local source, attached to **Apply NR to the finished picture**. That option rejects DX11 bridges. Its saved `FinishedPicture=auto` currently resolves to false. The user places the warning in Home/ReShade; the precise visible line was not captured, so this source match remains a candidate explanation, not confirmed attribution. Luma's separate native API warning says it supports DX11, not DX12. Do not disable compatibility checks or change rendering APIs based on this report.
- After verifying Batman/helpers were closed, took private `menu-readability-20260913-221702`: both original INIs, logs, game and Steam configuration, library state, display/driver evidence, binary hashes/versions and driver-profile evidence. Prior working and failed snapshots remain separate. Exclusive file handles protected the two INI edits; no binaries, renderer, DLSS settings, launch options, saves or global settings changed.
- Enlarged ReShade STYLE FontSize and EditorFontSize 13→18, retaining FontScale=2; set OptiScaler Menu FontSize=18 and Scale=2.0. The configured menu font target is approximately 36 pixels. Readback/diff confirms only these four values changed. Font rendering/layout has not been checked in a fresh launch.
- Menu-only rollback: close Batman/helpers and run the private snapshot's `Restore-Menus.ps1`. Backup and after-edit hashes are checked; dry run passed. It preserves current files and refuses to overwrite subsequent settings changes. The older full-profile `Restore-LumaTrial.ps1` still targets the previous Steam library and requires path/ownership revalidation before use.
- Activation guidance: DLAA and NR are already enabled; F12 exposes compact NR controls, F10 toggles NR, F11 selects numeric model presets, F7 toggles NR dimensions 25%/100%, F8 changes before/after SR; Save Settings persists OptiScaler selections. Quality/Balanced/Performance and F6 remain unavailable; FG/MFG and RR remain off for the previously documented integration limits. Next boundary: next-launch menu readability and exact warning identification if it persists. No game launch, scheduled follow-up or new graphics experiment.

## 2026-09-13 — Luma real-input DLAA + OptiScaler NR installed and tested

- User selected all compatible DLSS features working together. Presented the Luma → OptiScaler route and the alternative RenoDX consumer route; proceeded with one OptiScaler NR owner after establishing the shared Luma foundation. No second NR/FG/swapchain owner, no GitHub publishing, no other game changes.
- Preserved the user-confirmed working disabled-hook baseline immediately before mutation in private `luma-trial-20260913-204147`: full binary directory, game/Steam configuration, saves, driver-profile evidence, hashes and environment. Kept the separate failed `repair-20260913-192733` state. A second complete `before-opti-20260913-205405` checkpoint preserves Luma alone. A first checkpoint attempt stopped on a missing evidence JSON before game mutation; the repeated complete checkpoint succeeded.
- Installed official Luma Batman build 649 and its bundled ReShade, then the locally rebuilt wilsjo2 OptiScaler v0.7.7 `e237f89+f7-25-100`. Ownership: OptiScaler `dxgi.dll` loads `ReShade64.dll`, which hosts `Luma-Batman Arkham Knight.addon`; matched NR forwarder + signed NR 310.8. Package SR 310.6 was superseded at runtime by NVIDIA's signed cache 310.9. The old `winmm.dll`, ReShade proxy and Feeder addon remain `.repair-disabled`. No RenoDX consumer. Empty `LumaBase.ini` means the old optical-flow/Feeder shaders are not active techniques, although ReShade still compiles those present source files.
- Followed the official OptiScaler Luma chain settings: `Dx11Upscaler=dlss_12`, `LoadReshade=true`, `CreateD3D12DeviceForLuma=true`, `DontUseNTShared=true`; disabled spoofing and all OptiScaler FG paths. ManualInputPolling supports the local controls. The update banner reports a newer upstream version; this is not authority to replace a working custom build.
- Tested bundled ReShade alone, then Luma alone, then Luma + OptiScaler DLAA with NR off. Reached main menu and the existing 2% Batmobile checkpoint on Bleake Island. Luma-only normal exit took about 31 seconds and completed. During host-only testing Delete on save selection opened the game's delete confirmation; selected No, with no save deletion. Do not test that key on save selection.
- **Actual DLSS contract:** successful D3D12 native DLSS feature creation through the DX11 bridge; 3840×2160 input/output, quality 5 DLAA, jitter count 8, engine motion/depth/color resources delivered, motion scale 1920×1080, auto exposure/HDR/jittered-motion flags. Repeated DLSS evaluations and presentations succeeded. This is the engine-input route from Luma, not synthetic Feeder optical flow. No native lower-resolution SR quality adapter is present.
- **NR live test:** F10 enabled NR through the game's DLSS input at 20:57:11. Logs confirm real NR model creation, guide metadata, linear color conversion, evaluation and residual composition. Default after-SR target 3840×2160/model 960×540; F7 recreated 3840×2160 NR and returned to 960×540. F8 changed actual execution before SR, then back after SR. F11 completed 0→1→2→3→0 with recreation. F10 off/on passed. Delete full menu and F12 compact menu opened/closed. Save Settings persisted NR true and scale 0.25 at 20:59:32.
- Approximate parked-scene observations at 4K: DLAA with NR off 54–55 FPS; 25% NR about 45 FPS and 8.5 GB VRAM; full-resolution NR about 28 FPS and 9.4 GB VRAM. No controlled benchmark claim. Scene/UI were visible, camera distance changed with F, and pause worked; full traversal/combat, resolution recreation and alt-tab were not established. Home is configured for the separate host but automated input did not open it; Page Up/Down are configured, not live-cycle verified. The compact panel correctly reports live SR shortcut unavailable.
- First combined run exited normally at 21:01:40; Steam recorded exit code 0. Before the final settings edit, closed game/Steam/helpers and copied the live INI to a dated checkpoint. Reasserted explicit hotkeys, disabled FG, NR true/0.25/preset 0/one pass/after SR, and reduced file logging from debug to info. Fresh launch at 21:04:05 reached title/save selection with DLSS creation success and NR dispatch at 21:05:07 using the saved settings; no startup access violation.
- **Feature limits:** Luma captures engine inputs but currently requests 1:1 DLAA, with dynamic resolution disabled. F6 Quality/Balanced/Performance is not implemented. RR lacks a ray-tracing integration and its required signals. Current runtime reports scRGB presentation; NVIDIA's [Streamline 2.14.1 section 11](https://github.com/NVIDIA-RTX/Streamline/blob/v2.14.1/docs/ProgrammingGuideDLSS_G.md#110-dlss-g-and-hdr) explicitly excludes FP16/scRGB from DLSS FG. The fork contains DX11 FG bridging code, so D3D11 alone is not the verdict; the active presentation contract is the concrete incompatibility. FG/MFG payload was not installed or enabled. A supported-format conversion/bridge would require separate implementation and validation. MaxFPS remains 90; no limit edit.
- **Saved-profile gameplay recheck:** the fresh NR-on launch loaded the same Batmobile checkpoint, showing 45–46 FPS. F12 displayed enabled, preset 0, scale 0.25, one pass, Before SR unchecked, and unavailable live SR adapter. NR timing/evaluation continued without errors. This confirms startup-to-gameplay persistence rather than only an INI edit.
- Final fresh-run shutdown completed at 21:09:27; Steam recorded exit code 0. Preserved final logs/settings and the active file hash manifest in private `fresh-nr-on-clean-exit`; game left closed, installed profile remains enabled for the next launch. Curated documentation local links and private-path checks passed.
- **Rollback and next boundary:** private guarded `staging/batman-repair/Restore-LumaTrial.ps1`, after closing Batman, Steam and helpers. It validates backup hashes and contained ownership paths, preserves live trial files/settings/logs, and restores the disabled-hook working baseline. Dry-run passed after both installations and final config; rollback not executed. Do not use the old Full/F7 restore path on the Luma layout. Keep this exact short-test-successful profile, with Luma's upstream unfinished status explicit; next new validation is sustained moving gameplay and display recreation, not another consumer installation. See [sanitized hashes/evidence](../../evidence/BATMAN-ARKHAM-KNIGHT.txt).

## 2026-09-13 — Luma's feature scope and RenoDX consumer comparison

- User asked whether ReShade + Luma + a RenoDX neural consumer is preferable, or whether Luma includes its own DLSS 5. Current [Luma DLSS implementation](https://github.com/Filoppi/Luma-Framework/blob/main/Source/Core/dlss/DLSS.cpp) creates/evaluates D3D11 SuperSampling; the inspected Batman path and shared implementation contain no DLSS-NR integration. Distinguish its DLSS reconstruction from DLSS 5 neural rendering. This does not establish Batman render-scale/quality-mode completeness.
- The [NIGos bridge documentation](https://github.com/NIGos/dlss5-bridge) describes mirroring D3D11 engine color/depth/motion NGX inputs into D3D12 for a compatible separate neural consumer, including mod-provided DLSS. This supports a possible Luma + bridge + Krish-style RenoDX DLSS5 route, not tested Batman compatibility. Keep the substitute/optical-flow path off when using Luma's DLSS inputs.
- RenoDX build identity matters: [An0sTheGreat's current distribution](https://github.com/An0sTheGreat/DLSS-5-Super-Anus) documents experimental direct DX11 DLSS input capture through a private DX12 consumer. Such a consumer may not need NIGos' additional bridge; do not stack bridges by default. [RenoDX issue 651](https://github.com/clshortfuse/renodx/issues/651) reports ShortFuse input matching failing with bridge-sourced DLAA in another game, so a loaded neural effect alone does not prove use of real inputs.
- No local evidence ranks RenoDX above OptiScaler DLSS-NR for Batman's stability, cost or quality. Recommendation remains: establish ReShade + Batman-specific Luma alone in a user-selected future trial, then compare one compatible neural consumer at a time. No installation, launch, FPS edit or hotkey change; current playable profile remains unchanged.

## 2026-09-13 — Luma to OptiScaler feasibility without RenoDX

- Read-only follow-up: RenoDX is not a dependency of the proposed Luma-to-OptiScaler route. Luma uses ReShade as its host. The [official OptiScaler Luma UE guide](https://github.com/optiscaler/OptiScaler/wiki/Luma-Unreal-Engine-Luma-UE) documents DLSS inputs from Luma and supported load arrangements; this establishes general interoperability, not validation of Batman's separate unfinished integration.
- The preserved Dagherbou v0.2.0 source and [pinned upstream DX11 feature provider](https://github.com/Dagherbou/OptiScaler_DLSSNR/blob/v0.2.0-dlssnr/OptiScaler/upscalers/FeatureProvider_Dx11.cpp) expose the DLSS-on-12 backend for DLSS plus NR. Its [DX11/DX12 bridge](https://github.com/Dagherbou/OptiScaler_DLSSNR/blob/v0.2.0-dlssnr/OptiScaler/upscalers/IFeature_Dx11wDx12.cpp) invokes NR after successful upscaling, before copying the output back to DX11. Plain native-DX11 DLSS is not this NR path.
- Candidate only: Batman-specific Luma/ReShade supplies engine inputs, then OptiScaler DLSS-NR consumes them through the bridge; Feeder and its optical-flow provider are unnecessary for this route. No Batman runtime compatibility or crash fix is established. A selected future trial must establish Luma alone before adding OptiScaler/NR. No downloads, installation, settings changes or game launch occurred; the user-confirmed working disabled-hook profile remains the next starting boundary.

## 2026-09-13 — Stock success confirmed; Luma and FPS-cap research

- The user confirms the game works with the graphics components disabled. This establishes a user-confirmed working profile, not a completed sustained gameplay/performance test by the agent.
- Corrected the earlier incomplete mod search: [Luma's Batman source](https://github.com/Filoppi/Luma-Framework/blob/main/Source/Games/Batman%20Arkham%20Knight/main.cpp) captures the game's R16G16_FLOAT motion resource, scene color and depth, patches projection jitter, and replaces SMAA passes with a temporal reconstruction call. This is code evidence of an engine-input integration, distinct from Feeder optical-flow estimation; completeness and visual quality remain untested here.
- [Build 649](https://github.com/Filoppi/Luma-Framework/releases/tag/latest-649), published September 12, includes Batman release/test packages. The [wiki](https://github.com/Filoppi/Luma-Framework/wiki) marks Batman work in progress and DLSS only/no HDR. [Issue 153](https://github.com/Filoppi/Luma-Framework/issues/153) describes smearing/flicker on RTX 5070 Ti; it was closed September 1 with the maintainer saying the mod is not finished, not that the issue was fixed.
- Do not infer Quality/Balanced/Performance, dynamic render scaling or compatibility with DLSS-NR from the presence of the DLSS call alone. Source sets dynamic_resolution=false. Luma has not been installed; a future trial must use a separate reversible profile with Feeder disabled and establish Luma alone first.
- Read-only local inspection found BmSystemSettings.ini line 30: MaxFPS=90.000000. The [120 fps mode author](https://www.nexusmods.com/batmanarkhamknight/mods/3269) supplies an INI-based 120/144 option. Prefer changing the existing MaxFPS setting over overwriting the whole configuration. No cap or game settings changed in this research turn.

## 2026-09-13 — Startup crashes preserved; diagnostic stock profile selected

- Windows Application events recorded three BatmanAK.exe access violations, exception c0000005 in system d3d11.dll at offset 0x17bb82, at 18:11:54, 18:12:43 and 19:20:08 local time. The rendering-thread report includes ReShade dxgi.dll and OptiScaler winmm.dll in the stack. This implicates the active presentation chain but does not isolate a root cause.
- Last full-stack logs confirm SDR 3840x2160, Lumenite Kernel compiled/enabled, Feeder NGX routing, successful DLSS creation, NR composition at 3840x2160 and at least three delivered frames. These are startup observations, not gameplay stability evidence or proof of correct depth/motion.
- Preserved the just-in-time private snapshot `repair-20260913-192733`: complete Win64 files, settings, game dumps, Windows reports, Steam/app/save state, graphics environment and driver-profile copies, with hashes and versions. Older stock/F9/F7 snapshots remain separate. Steam shutdown requests did not finish; after checking no Steam game executable was running, its client/UI processes were stopped before file changes.
- Guarded `staging/batman-repair/Switch-TestProfile.ps1 -Profile Stock` passed dry run and renamed only dxgi.dll, winmm.dll and dlss5-feed.addon64 with `.repair-disabled` suffixes. All three are confirmed disabled. Original game files and settings were not replaced. This is a later diagnostic profile; do not run older rollback scripts against its renamed files without reviewing the state.
- Launched through Steam. Agent observed stock startup; Escape stopped Computer Use before menu verification, so no further input or profile change was made. The next turn's user confirmation supplies the successful-launch result above.
- Exact diagnostic reversal, only when explicitly resuming testing: close Batman/Steam/helpers and run the same guarded switch script with `-Profile Full`. It verifies the binaries against the repair snapshot and saves current logs/configs first. Full is a known-failing profile, not the default recommendation. No reminder scheduled.

## 2026-09-13 — F7 two-value controls installed

- User requested replacing every installed NR resolution cycle with F7 toggling only 25% and 100%, and live DLSS SR selection beside NR where possible.
- Same game builds, rendering APIs, RTX 5070 Ti/616.56 and existing hook owners as the preceding inventory; live desktop 3840×2160 at 150 Hz. No game or helper was running; Steam was shut down gracefully before mutation.
- Current-state snapshot: private `f7-controls-20260913/before-f7-20260913-141908`, with before/after file hashes, root hook/executable version/hash inventory, INIs, CET configuration, UserSettings, per-app Steam state, launch configuration, NVIDIA DRS and graphics environment. Prior stock/known-good snapshots remain separate.
- Full rebuilds used MSVC 14.44.35207 / Windows SDK 10.0.26100.0. Both preserve all 946 named exports/ordinals. [Follow-up source patches](../../examples/optiscaler-dlssnr/f7-two-scale/README.md) preserve the exact code changes without binaries.
- New proxy `F39E798B293A1F4BEBE10F0EED781C4B7AB4E6AFA6357030CF127314AA57517D`, stamp `9737616+f7-25-100`. ScaleCycleKey=0x76, initial WorkingScale=1.0. Existing release/focus handling, notification and 750 ms cooldown retained.
- Same binary passed two isolated 300/300-evaluation helper runs, exit 0. Composition confirmed model 640×360 (100%) and 160×90 (25%); no error/critical log lines. The generic running-size log still says 640×360 at both scales, so composition is the scale evidence. Tests used separate processes, not live keyboard cycling.
- Changed winmm.dll, OptiScaler.ini and UserInput.ini. The latter removes the inherited `F7 -> Stat StopFile` binding through a reversible UE input override. Native live SR and pre/post-SR remain unavailable; no consumer migration. Original staged payload/manifests remain historical and must be used only after Restore-F7.
- Verification: target hashes matched after installation, protected native files unchanged; restore dry-run passed before and after mutation. No new game launch or live keyboard test. Earlier runtime evidence is historical, not proof for this revision.
- Rollback: close games/Steam/helpers; run the snapshot's `Restore-F7.ps1 -Game Batman`. It validates targets/backups, preserves the current files and restores only this revision's changes. Run it before older rollback scripts because those expect the previous hashes. No rollback executed.
- Next boundary: verify F7 key release, notification, actual 25%/100% model sizes, recreation and Save Settings reload in a repeatable scene. Verify effective game input overrides and scene depth/motion first; this helper does not prove Arkham gameplay. No reminder or automatic resume scheduled.

## State recorded before the F7 revision

- Installed: ReShade 6.8.0 + Feeder 1.16.0-beta.1 + Dagherbou OptiScaler DLSS-NR v0.2.0 with local `9737616+f9-scale` customization + signed NVIDIA 310.8 runtimes.
- Baseline: fresh stock installation, Steam build 13250859, existing cloud saves preserved; gameplay baseline not yet tested.
- Resume boundary: first stock/game rendering and depth/motion validation; do not claim gameplay success from the helper test.
- Reminder policy: none requested or scheduled.

## 2026-09-13 — Controls promoted to defaults for all graphics add-ons

- After the F9 installation, the user requested the same installation/control approach as the default regardless of which add-on is used.
- Updated general preferences, agent instructions, installation guidance and the preference template to apply the supported controls across implementations. The new general F9 cycle is 100/75/67/50%; F8 requests actual pre/post-SR placement and F7 requests actual upscaler quality modes.
- Preserved existing build behavior and evidence, including the older v0.7.7 100/75/50/25% cycle. This was a preference/documentation update; no game files or installed profiles changed. Batman's F8/F7 limitations and pending in-game checks remain unchanged.

## 2026-09-13 — F9 NR model-resolution control installed

- User authorized implementing the feasible controls. Added F9 to the existing documented consumer; no RenoDX migration was needed. F8 pre/post-SR and F7 native SR quality remain unbound for the reasons in the earlier reviews.
- Built from the exact v0.2.0 tag, commit `973761621353b99bee3dc7d4bb27b117fef2644f`, with matching pinned external dependencies. Local resource stamp: `9737616+f9-scale`. The unrelated custom consumer used for another game was only read as a source of matching build dependencies.
- F9 cycles 100%, 75%, 67%, 50%, then 100% using the existing live working-scale setting. It follows the existing key-release/focus/capture input path, displays the selected percentage, and limits changes to one per 750 ms. The key is rebindable under **NR Model Resolution**; **Save Settings** persists the working scale and binding.
- Build succeeded; 11 compile-time scale boundary checks passed. The rebuilt DLL retains all 946 original named exports with their ordinals unchanged. Source patch and tests are preserved under `examples/optiscaler-dlssnr/dagherbou-v020-f9/`.
- The exact rebuilt DLL passed four isolated helper runs, each 300/300 evaluations, exit 0, native DLSS creation and NR feature 18 confirmed. Composition logs confirmed model dimensions 640×360, 480×270, 429×241, and 320×180 for the four scales. No OptiScaler error/critical lines or evaluate failures were found in these runs.
- Those tests use fixed initial scales in separate processes. They do **not** validate live keyboard input, repeated resource recreation in a running game, notification visibility, scene depth/motion, or gameplay stability.
- Another game was running at installation time. Its files and process were left alone. Batman and its helpers were absent; Steam, its UI processes and the running game had no Batman modules; both target files passed exclusive read/write-open checks; Steam reported Batman fully installed at the recorded build. Only unloaded, process-local Batman files were replaced. The initial overly broad installer preflight also tried to inspect the protected Steam service and stopped before any game write; the final guard scopes module checks to the user processes that can host these graphics hooks, retains exclusive-file checks, and checks the game/build again immediately before writing.
- Immediately before mutation, preserved `before-f9-20260913-132152`: current binary/configuration files, original install metadata, external Steam/cloud-save/localconfig state, graphics environment and driver profile database copies. Original stock snapshot and successful original helper baseline remain separate.
- Changed only `winmm.dll` and `OptiScaler.ini`. Installed DLL SHA-256: `F5730AA7BB385F0B3CFD78D056529F8F491A030084872F039F6BA2B97BA10590`. `ScaleCycleKey=0x78`; initial `WorkingScale=1.0`; F10 remains `0x79`. All 35 owned files match the updated manifest, and the other snapshot binary-directory files remain unchanged.
- Both rollback paths passed dry runs. `Restore-Before-F9.ps1` preserves the current F9 files and restores the previous DLL/INI and their manifests; `Restore-Stock.ps1` disables the full owned stack. Close Batman, Steam and helpers before using those profile scripts. No rollback was executed.
- Next boundary: first stock/full-stack game test, then F9 key release, percentage notification, each working size, repeated cycling and saved-settings reload in a repeatable scene. No reminder or automatic game launch was scheduled.

## 2026-09-13 — RenoDX alternative and hotkey question

- User asked whether switching to RenoDX changes the F9/F8/F7 feasibility. This was a review, not an instruction to replace the installed stack; no game files changed.
- Distinguish Krish's `renodx-dlss5` + Feeder from ShortFuse's standalone `renodx-dlss`. Feeder's current documentation describes the latter as a standalone option for x64 D3D11 and says not to combine the two input providers.
- [DLSSNR-Cost-Scaler](https://github.com/xenmods/DLSSNR-Cost-Scaler) documents NR-only scaling and live INI reloading, tested by its author with ShortFuse's add-on over D3D12. This supports the feasibility of an F9-style controller for an appropriate chain, but does not verify Arkham Knight or a built-in F9 binding.
- RenoDX-derived distributions have different control and API scopes. The current [An0sTheGreat distribution](https://github.com/An0sTheGreat/DLSS-5-Super-Anus) documents NR scale controls primarily for DX12 and experimental DX11 native-DLSS input capture; do not assume its scaling support applies to Arkham Knight solely from the RenoDX name.
- No current local evidence establishes F8 pre/post-SR switching or F7 native Quality/Balanced/Performance controls through a standalone RenoDX build in Arkham Knight. Replacing the neural consumer alone does not supply an engine render-resolution/jitter integration.
- Any future RenoDX test must preserve and disable the current OptiScaler neural consumer first; use one documented input provider and one neural consumer, then verify actual depth/motion, NR evaluation and output delivery.

## 2026-09-13 — F9/F8/F7 control feasibility review

- User asked about F9 for neural model resolution, F8 for before/after-SR placement, and F7 for upscaling quality levels, including persistence in presets.
- Read-only game inspection: installed Dagherbou v0.2.0 retains `WorkingScale=1.0` and its original proxy hash. No new hotkeys or game configuration changes made during this review.
- F9 is implementable by adding an input handler for the existing live `DlssNrWorkingScale` setting. The current menu already exposes Model resolution; this scales the NR work independently of game resolution and Feeder's overall work resolution. Suggested cycle values are proposals, not saved defaults.
- F8 needs a before-SR implementation absent from the installed v0.2.0. The separately preserved wilsjo2 v0.7.7 source has `DlssNrRunBeforeSr`, but its Feeder compatibility must be validated before replacing this game's fork. Current Feeder uses a 1:1 DLAA contract, so pre/post placement would not acquire a smaller engine-rendered input merely by changing order.
- F7 cannot create native DLSS Quality/Balanced/Performance modes in this game. Feeder's `work_resolution` plus spatial expand-back is a distinct possible control: it reduces processing of an already rendered frame. Its synthetic-jitter SR option is experimental and upstream advises against it based on cost and shimmering observations.
- Persistence can use named configuration profiles or OptiScaler's saved settings. NVIDIA numeric model preset hints are separate from working scale and SR quality; do not silently bundle them or claim a ReShade preset edits OptiScaler's INI.
- Next boundary remains unchanged: review/choose any fork migration separately and preserve the installed profile before implementing controls.

## 2026-09-13 — Requested OptiScaler with DLSS 5

- Recovered repository and recent task context; no earlier Batman record was found.
- User chose OptiScaler + neural rendering explicitly. Added the currently documented ReShade/Feeder dependency to supply Arkham Knight's missing NGX contract, using exactly the fork named by Feeder upstream.
- Prepared 35 process-local files, isolated ReShade `dxgi.dll` from OptiScaler `winmm.dll`, kept one neural consumer, disabled OptiScaler FG and unrelated spoofing/input hooks.
- Preflight in a private helper: 640×360 synthetic DLAA; 300/300 evaluates; native DLSS create success; NR dispatch and feature 18 confirmed; exit 0.
- The helper's early feature-18 requirements query returned NotImplemented and warned that fallback was possible. Later OptiScaler logs confirmed `DLSSFeatureDx12::InitDLSS _CreateFeature result: NVSDK_NGX_Result_Success`; do not report that warning as an actual FSR fallback.
- A downloaded NR candidate had a signature hash mismatch and was not used. The installed NR runtime is the original signed `E16BCF…FC8E` file; the source installation was only read.
- Immediately before game writes, preserved private snapshot `stock-before-install-20260913-124918`, including native binaries, configs, Steam/app state, cloud saves and driver profile database files. Kept hashes, payload manifest and restore procedure outside published paths.
- Installed-file hashes matched. All 25 native files in `Binaries\Win64` remained unchanged. Upstream verifier: 19 OK, 0 warnings, 0 failures.
- No game launch performed. Scene depth, shader compilation, transport/presentation, image quality, hotkeys, effective resolution, HDR, frame pacing and game stability remain unverified. No actual game failure recorded.
- Rollback prepared and dry-run checked; no rollback performed. Close game/Steam/helpers and run private `Restore-Stock.ps1` to archive the owned mod files without changing stock files or saves.
- Reusable lesson: current upstream explicitly allows its documented OptiScaler DLSS-NR consumer; ordinary OptiScaler is still incompatible. Updated the general compatibility documentation without migrating earlier experiments.

## 2026-09-14 — Library comparison review

Added [categorized comparisons](COMPARISONS.md) covering manager/foundations, performance, graphics/DLSS, QoL/UI, animation, AI, abilities/mechanics and actual content extensions. No game files changed or new gameplay tests were performed by this review. Earlier local evidence remains stronger than generic recommendations; historical control mappings and unresolved validation boundaries are preserved. See the [14-game guide](../../docs/INSTALLED-GAMES-MOD-GUIDE.md) for scope and installed-library checks.
