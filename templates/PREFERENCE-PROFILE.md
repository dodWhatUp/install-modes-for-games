# <Scope> Preferences

Last confirmed: YYYY-MM-DD

## Always prefer

## Prefer when compatible

Inherit the [general DLSS and ReShade/RenoDX defaults](../preferences/GENERAL.md): maximize useful features using real inputs, establish the minimum baseline, reuse existing capabilities, and add only missing dependencies. Evaluate the game-specific visual profile separately and validate it with the DLSS stage. OptiScaler is not mandatory. Adjust links when copying into a game's folder.

- Game-specific input integration and minimum stack:
- Selected extensions and required extra components:
- ReShade/RenoDX profile, supported load chain, or exact compatibility blocker:
- Explicit game-specific exceptions to these defaults:

## Graphics control mapping

Inherit [general defaults](../preferences/GENERAL.md) using the [installation procedure](../docs/GRAPHICS-CONTROLS.md). Adjust these relative links when copying the template into a game's folder.

- Implemented keys and owning component:
- Unsupported actions and reasons:
- Game/overlay conflicts and explicit exceptions:
- Settings/profile save mechanism:
- Configured / helper-tested / tested in game:

## Ask before

## Avoid

## Hardware/build constraints

## Superseded preferences

Record the date and reason; do not delete history silently.
